import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    sortOption: {
        fontSize: 16,
        marginRight: 20,
        margin: 10,
        color: 'white',
    },
    selectedOption: {
        fontWeight: 'bold',
    },
});
